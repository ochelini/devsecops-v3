version: "3.8"

services:

  user-service:
    build: ./user-service
    ports:
      - "8081:8080"
    networks:
      - app-network

  order-service:
    build: ./order-service
    ports:
      - "8082:8080"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
